import 'package:flutter/material.dart';

class DrawingAR extends StatelessWidget {
  const DrawingAR({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("AR"));
  }
}
